# ci-build — this file is yours.
#
# gt created it once and will never modify or delete it again.
#
# shellcheck over every shell script, moved from ci.yml verbatim.
name: ci-build

on:
  workflow_call:

permissions:
  contents: read

jobs:
  lint:
    name: shellcheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install shellcheck
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends shellcheck

      - name: Lint all shell scripts
        run: |
          set -euo pipefail
          # -x follows `source` directives so library functions resolve.
          shellcheck -x -S style lib/*.sh services/*/*.sh
